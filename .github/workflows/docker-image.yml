name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build -t github.com/pablomarquezfernandez/censojaveriana/censojaveriana:v1.0 .
   
    - name: Tag the Docker image
      run: docker tag github.com/pablomarquezfernandez/censojaveriana/censojaveriana:v1.0 github.com/pablomarquezfernandez/censojaveriana/censojaveriana:v1.0

    - name: Push the Docker image
      run: docker push github.com/pablomarquezfernandez/censojaveriana/censojaveriana:v1.0
